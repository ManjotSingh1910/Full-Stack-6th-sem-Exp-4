# Context API App

Features:
- User authentication context (login/logout)
- Theme toggle context (persisted in localStorage)
- Language preference context (persisted in localStorage)

Run:

```bash
cd "context-app"
npm install
npm run dev
```
